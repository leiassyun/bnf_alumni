<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <title>NYU Blockchain & Fintech Club Alumni Database</title>
    <link href="https://maxcdn.bootstrapcdn.com/bootstrap/4.5.2/css/bootstrap.min.css" rel="stylesheet">
</head>
<body class="bg-light">
    <div class="container mt-5">
        <h3 class="text-center mb-4">NYU Blockchain & Fintech Club</h3>
        <h1 class="text-center mb-4">Alumni Database</h1>
        <a href="/add" class="btn btn-primary mb-3">Add New Alumni</a>
        <ul class="list-group">
            <% alumni.forEach(alumnus => { %>
                <li class="list-group-item d-flex justify-content-between align-items-center">
                    <span><strong><%= alumnus.name %></strong> - <%= alumnus.phoneNumber %></span>
                    <div>
                        <a href="/edit/<%= alumnus._id %>" class="btn btn-warning btn-sm">Edit</a>
                        <form action="/delete/<%= alumnus._id %>" method="POST" class="d-inline">
                            <button type="submit" class="btn btn-danger btn-sm">Delete</button>
                        </form>
                    </div>
                </li>
            <% }) %>
        </ul>
    </div>
    <script src="https://code.jquery.com/jquery-3.5.1.slim.min.js"></script>
    <script src="https://cdn.jsdelivr.net/npm/@popperjs/core@2.5.2/dist/umd/popper.min.js"></script>
    <script src="https://maxcdn.bootstrapcdn.com/bootstrap/4.5.2/js/bootstrap.min.js"></script>
</body>
</html>
